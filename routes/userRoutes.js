
// const healthController = require('../controller/healthController.js');

// const router = require('express').Router();

// const handleInvalidMethods = (req, res, next) => {
//     if (req.method !== 'GET') {
//       // Send a 405 Method Not Allowed response
//       res.status(405).send('Method Not Allowed');
//     } else {
//       next(); // Continue to the healthController.health handler for GET requests
//     }
//   };
  
//   router.route('')
//     .all(handleInvalidMethods) // Apply middleware to handle invalid methods
//     .get(healthController.health); // Handle GET requests

// module.exports = router; 
